{
  "name": "acii-functions",
  "description": "Cloud Functions do Portal ACII — hoje só espelha auth_links (Firestore) em Custom Claims do Firebase Auth, pra permitir que o Storage Rules cheque papel/setor sem precisar de cross-service rules (que não funcionam com o banco Firestore nomeado deste projeto).",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.1"
  },
  "private": true
}
